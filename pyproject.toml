[project]
name = "whole-body-benchmark"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.8.*"
dependencies = [
    "nibabel>=5.1.0",
    "protobuf==3.20",
    "scipy>=1.10.1",
    "tensorflow-gpu==2.6.0",
    "keras==2.6.0",
]
